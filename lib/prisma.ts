import { PrismaClient } from '@prisma/client'

/**
 * @deprecated This constant is deprecated and will be removed in future versions.
 */
export const prisma = new PrismaClient()
export const prismaClient = prisma
